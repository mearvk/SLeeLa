#sleela 1.1

class TimePiece {
    void main() {
        timeSetLocation("DE", "Europe/Berlin");
        print(timeUtcMillis());
        print(timeMonotonicNanos());
        print(timePrecisionMillis());
        print(timeLocation());
        print(timeHttpDate());
        print(timeJson());
    }
}
