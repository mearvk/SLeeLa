# SLeeLa Reflection

Portable C++17 runtime type metadata registry. Types may be registered with size, alignment, fields and method signatures. C++ does not automatically expose arbitrary source-level reflection here; metadata is explicitly registered/generated so the contract is deterministic and portable.
