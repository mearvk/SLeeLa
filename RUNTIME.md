# SLeeLa Runtime

C execution core with operand stack, constants, globals, locals, call frames, opcode dispatch, arithmetic, comparisons, control flow, calls/returns, results, bounded threads, locks and message passing. Runtime services cover memory limits, processes, files, sockets, time, terminal/PTY and dynamic libraries through OS adapters.

Resource exhaustion is explicit failure. ABI behavior is separated from compiler behavior.

**Max Rupplin — MEARVK LLC — 2026**
