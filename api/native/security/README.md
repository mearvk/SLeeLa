# Native Security Foundation

Provides secure memory zeroization, SHA-256 digesting, constant-time byte comparison, and credential cleanup.

This package does not claim to implement TLS, certificate validation, key storage, secure random generation, or a complete secrets vault. Those capabilities require explicit cryptographic/backend integration and platform policy.