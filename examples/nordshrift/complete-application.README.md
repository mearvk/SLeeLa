# Nordshrift Complete Application Example

This demonstrates the intended architecture: one Nordshrift control sheet references canonical Sleela-Complete classes and uses the shared Sleela lowering/core-artifact path.

This is a contract/example. It does not claim that the generic SST parser already accepts every field shown here; the implementation must adopt the repository's actual SST grammar rather than silently inventing a second grammar.
